$('.brands-slider').slick({
    infinite: true,
    slidesToShow: 4,
    slidesToScroll: 1,
    autoplay: true,
    autoplaySpeed: 1500
  });

  
$("#open_window").animatedModal({
    animatedIn:'zoomIn',
    animatedOut:'bounceOut',
    color:'#337AB7'
});